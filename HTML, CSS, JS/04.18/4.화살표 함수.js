// An arrow function that receive two parameters to multiply the two numbers and return the result.

// let x = function(x, y) {
//   return x * y;
// };

const x = (x, y) => x * y;
document.write(x(5, 5));

// An arrow function that receive two parameters to sum the two numbers and return the result.
const xPlusY = (x, y) => x + y;
document.write(xPlusY(5, 5));